// Build a Binary tree
#include<bits/stdc++.h>
using namespace std;

struct node{
    int data;
    node* left;
    node* right;
};

int main(){
    node *root = (node*)malloc(sizeof(node));
    cout<<"Enter the root node: ";
    cin>>root->data;
    while (1)
    {
        
    }
    
}