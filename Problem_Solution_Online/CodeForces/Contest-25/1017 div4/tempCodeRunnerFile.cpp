#include<bits/stdc++.h>
using namespace std;

#define ll long long

void solve(){
    int n, m, l, r;
    cin >> n >> m >> l >> r;
    
    
    int left = -m / 2;          
    int right = m - m / 2;      
    
    cout << left << " " << right << "\n";
}
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    int t; // number of test cases
    cin >> t;
    while (t--)
    {
        solve();
    }
    
}